import { BoxedDirective } from './boxed.directive';

describe('BoxedDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxedDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
